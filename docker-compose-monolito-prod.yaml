name: gotodolist-monolito

services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-gotodolist}
      POSTGRES_USER: ${DB_USER:-gotodolist}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-replace-me}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 5s

  monolito:
    image: ghcr.io/${GHCR_OWNER:-elton-bt}/gotodolist-monolito:${IMAGE_TAG:-latest}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      APP_PORT: 8080
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: ${DB_NAME:-gotodolist}
      DB_USER: ${DB_USER:-gotodolist}
      DB_PASSWORD: ${DB_PASSWORD:-replace-me}
      DB_SSLMODE: disable
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:8080/health"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 15s

volumes:
  postgres-data: